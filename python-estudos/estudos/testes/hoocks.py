"""
    hoocks - São os testes em si. Ou seja, a execução dos testes.

    setup() -> é executado antes de cada método de teste. É util para criarmos instância de objetos e outros dados:

    tearDown() -> é executado ao final de cada método de teste. E útil para excluir dados ou fechar conexões com banco de dados
    
"""